
   --In this project everything is hardcoded except the password for the email address and the directory for the file.
If you want to test it you need to adjust it for your needs. 
   --Sending emails is hardcoded, usually is not suppose to be done like that, in real life scenario everything will be put in asset, orchestrator,config file, etc.
   --In real life scenario I would put all the regex expressions out of the code.